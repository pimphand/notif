# PostgREST configuration for Notif
# Run: postgrest postgrest.conf
# Requires: db-uri, jwt-secret (same as NOTIF_JWT_SECRET)

db-uri = "postgres://connect_id:1dR7V\xbU,U4@x+msTKj_@localhost:5432/notif"
db-schemas = "public"
db-anon-role = "notif"
db-pool = 10
server-host = "127.0.0.1"
server-port = 3001

# JWT secret (base64 or raw). Must match NOTIF_JWT_SECRET used by Rust app.
# Generate: openssl rand -base64 32
jwt-secret = "your-jwt-secret-at-least-32-chars"

# CORS (optional)
# server-cors-allowed-origins = "*"
